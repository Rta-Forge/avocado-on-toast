# Engine source — keep local only, never push to this repo
src/

# Local release staging (binaries belong on GitHub Releases, not in git)
dist/
staging/
*.tar.gz
*.zip
*.deb
*.rpm

# Checksums belong to a specific release (uploaded as release assets), not root
SHA256SUMS
SHA256SUMS.asc
SHA256SUMS.local

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
